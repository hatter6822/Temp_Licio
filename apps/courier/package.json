{
  "name": "courier",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "WS-R.15.4a — the Licio native Android courier: a Capacitor shell that loads the UNCHANGED web build inside the system WebView and carries the same LCAP packs over native radio links. No courier-only web fork, no parallel data model.",
  "type": "module",
  "scripts": {
    "sync": "cap sync android",
    "build:apk": "cap sync android && cd android && (./gradlew --offline assembleDebug || ./gradlew assembleDebug)",
    "build": "node ./scripts/check-no-fork.mjs && cap sync android && node ./scripts/check-no-fork.mjs --assets && cd android && ./gradlew assembleDebug",
    "test:unit": "cd android && ./gradlew :app:testDebugUnitTest",
    "verify:no-fork": "node ./scripts/check-no-fork.mjs"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/cli": "^8.5.1",
    "@capacitor/core": "^8.5.1"
  }
}
